spring:
  application:
    name: UrlShortener
server:
  port: 80
springdoc:
  swagger-ui:
    disable-swagger-default-url: true
    path: /swaggerDoc
management:
  endpoints:
    web:
      exposure:
        include: '*'
